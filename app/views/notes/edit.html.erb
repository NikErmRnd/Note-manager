<h1>Edit Note</h1>

<%= link_to 'Вернуться к заметкам', :back %>

<%= simple_form_for([:book, @note]) do |f| %>


    <%= render partial: "form", locals: {f: f} %>

    <p> <%= f.submit "Save"%></p>

<% end %>
