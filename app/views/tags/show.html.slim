br
br
br
= link_to 'Блокноты', books_path , class: "btn btn-primary"
br
br
h4 Выбранный тег:
h1 = @tag.name

- if @tag.books.empty?
  h2 Блокнотов нет
- else
  - @tag.books.each do |book|
    div
      h3 = book.name
      = link_to "Перейти", book , class: "btn btn-primary"